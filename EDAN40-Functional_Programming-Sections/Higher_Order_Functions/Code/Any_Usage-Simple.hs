any odd [2, 4, 6, 8]
-- False
{- This function is actually the application of `odd` to every element, which
   constructs a list of the same size of only Boolean values.
   That list is then logically ORed together.
 -}
