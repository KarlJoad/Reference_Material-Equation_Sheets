all even [2, 4, 6, 8]
-- True
{- This function is actually the application of `even` to every element, which
   constructs a list of the same size of only Boolean values.
   That list is then logically ANDed together.
 -}
