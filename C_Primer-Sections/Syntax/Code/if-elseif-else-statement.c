if (logical-predicate) {
	this-predicate-true-clause;
} else if (logical-predicate) {
	this-predicate-true-clause;
} else {
	all-predicates-false-clause;
}

/* Note that the ( and ) MUST be present in the statement to denote the predicate.
 * { and } can be omitted ONLY IF the clause has a SINGLE statement in it. */
