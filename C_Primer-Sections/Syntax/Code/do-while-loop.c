do {
	statement;
}
while(condition);

/* The { and } SHOULD ALWAYS be included for the body of a do-while loop, although
 * the language and compiler do NOT require them. However, the ( and ) ARE required
 * to surround the condition.
 * In addition, the ending semicolon ; at the end of the while IS mandatory. */
