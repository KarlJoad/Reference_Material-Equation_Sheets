def idInt(x : Int) : Int = x
// idInt takes a parameter with type Int and returns an value of type Int

def idStr(x : Str) : Str = x
// idStr takes a parameter with type Str and returns an value of type Str
